import Assert from 'assert';

const assert = Assert.strict;

describe(
    'Users dao',
    function () {
        before(function () {
            console.log('before');
        })
        beforeEach
    }
)
//function () y no ()=> por que hacemos uso del this.mocha
//La expresión regular que controla esto es: /(?=[A-Za-z0-9@#$%/^.,{}&+!=]+$)^(?=.*[a-z])(?=.*[A-Z])(?=.*[0-9])(?=.*[@#$%^&+!=])(?=.{8,}).*$/g